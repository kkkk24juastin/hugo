# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                    Hugo 配置文件 - PaperMod 主题                           ║
# ║                                                                           ║
# ║  官方文档: https://github.com/adityatelange/hugo-PaperMod/wiki            ║
# ║  站点地址: https://blog.kkkk24juastin.asia/                               ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           基础站点配置                                     │
# └───────────────────────────────────────────────────────────────────────────┘

# baseURL: 站点的基础URL地址
# 说明: 必须以 "/" 结尾，用于生成绝对链接
# 示例: "https://example.com/" 或 "https://example.com/blog/"
baseURL = "https://blog.kkkk24juastin.asia/"

# languageCode: 站点语言代码
# 说明: 用于 HTML lang 属性和 RSS feed
# 可选值: "en" (英语), "zh-cn" (简体中文), "zh-tw" (繁体中文), "ja" (日语) 等
languageCode = "zh-cn"

# title: 站点标题
# 说明: 显示在浏览器标签页和 SEO 中
title = "kkkk24 的博客"

# theme: 使用的主题名称
# 说明: 必须与 themes/ 目录下的主题文件夹名称一致
theme = "PaperMod"

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           分页设置                                         │
# └───────────────────────────────────────────────────────────────────────────┘

[pagination]
  # pagerSize: 每页显示的文章数量
  # 说明: 列表页面每页显示多少篇文章
  # 建议值: 5-20
  pagerSize = 5

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           Hugo 功能开关                                    │
# └───────────────────────────────────────────────────────────────────────────┘

# enableRobotsTXT: 是否生成 robots.txt 文件
# 说明: true = 生成, false = 不生成
# robots.txt 用于告诉搜索引擎哪些页面可以抓取
enableRobotsTXT = true

# buildDrafts: 是否构建草稿文章
# 说明: true = 包含 draft: true 的文章, false = 排除草稿
# 生产环境应设为 false
buildDrafts = false

# buildFuture: 是否构建未来日期的文章
# 说明: true = 包含日期在未来的文章, false = 排除
buildFuture = false

# buildExpired: 是否构建过期的文章
# 说明: true = 包含设置了 expiryDate 且已过期的文章, false = 排除
buildExpired = false

# enableEmoji: 是否启用 Emoji 表情支持
# 说明: true = 可以使用 :smile: 等 emoji 短代码
enableEmoji = true

# enableGitInfo: 是否启用 Git 信息
# 说明: true = 可以获取文章的 Git 最后修改时间等信息
enableGitInfo = true

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           最小化设置                                       │
# └───────────────────────────────────────────────────────────────────────────┘

[minify]
  # disableXML: 是否禁用 XML 最小化
  # 说明: true = 不压缩 XML 文件 (如 sitemap.xml)
  disableXML = true
  
  # minifyOutput: 是否最小化 HTML 输出
  # 说明: true = 压缩 HTML, 减小文件体积
  minifyOutput = true

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           输出格式配置                                     │
# └───────────────────────────────────────────────────────────────────────────┘

[outputs]
  # home: 首页输出格式
  # 说明: 必须包含 "JSON" 才能启用搜索功能
  # 可选值: "HTML", "RSS", "JSON"
  home = ["HTML", "RSS", "JSON"]

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                           导航菜单配置                                     │
# └───────────────────────────────────────────────────────────────────────────┘

[menu]
  # main: 主导航菜单
  # 说明: 显示在页面顶部的导航栏
  # 参数说明:
  #   - identifier: 唯一标识符
  #   - name: 显示的菜单名称 (支持 emoji)
  #   - url: 链接地址
  #   - weight: 排序权重, 数字越小越靠前
  
  [[menu.main]]
    identifier = "home"
    name = "🏠 首页"
    url = "/"
    weight = 1
  
  [[menu.main]]
    identifier = "archives"
    name = "📚 归档"
    url = "/archives/"
    weight = 2
  
  [[menu.main]]
    identifier = "categories"
    name = "📂 分类"
    url = "/categories/"
    weight = 3
  
  [[menu.main]]
    identifier = "tags"
    name = "🏷️ 标签"
    url = "/tags/"
    weight = 4
  
  [[menu.main]]
    identifier = "search"
    name = "🔍 搜索"
    url = "/search/"
    weight = 5
  
  [[menu.main]]
    identifier = "about"
    name = "👤 关于"
    url = "/about/"
    weight = 6

# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                        PaperMod 主题参数配置                               ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

[params]

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         基本信息设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # env: 环境设置
  # 说明: 设为 "production" 时启用以下 SEO 功能:
  #   - Google Analytics
  #   - OpenGraph 元标签
  #   - Twitter Cards
  #   - Schema.org 结构化数据
  # 可选值: "production" (生产环境), "development" (开发环境)
  env = "production"
  
  # title: 站点标题 (params 中的)
  # 说明: 用于 SEO 和社交分享
  title = "kkkk24 的博客"
  
  # description: 站点描述
  # 说明: 用于 SEO meta 标签和社交分享
  description = "kkkk24 的个人博客 - 技术分享与生活记录"
  
  # keywords: 关键词列表
  # 说明: 用于首页的 meta keywords 标签
  keywords = ["docker", "机器学习", "开发", "编程", "生活"]
  
  # author: 作者名称
  # 说明: 显示在文章元信息中
  # 格式: 字符串 "作者名" 或 数组 ["作者1", "作者2"]
  author = "kkkk24"
  # author = ["kkkk24", "Guest"]  # 多作者写法
  
  # images: 默认分享图片
  # 说明: 当文章没有封面图时, 用于 OpenGraph 和 Twitter Cards
  # 格式: 数组, 放在 static/ 目录下的图片路径
  # images = ["default-cover.png"]
  
  # DateFormat: 日期格式
  # 说明: Go 语言的日期格式, 参考 https://gohugo.io/functions/time/format/
  # 常用格式:
  #   - "January 2, 2006" = 英文格式, 如 "February 4, 2026"
  #   - "2006-01-02" = ISO 格式, 如 "2026-02-04"
  #   - "2006年01月02日" = 中文格式, 如 "2026年02月04日"
  DateFormat = "2006-01-02"
  
  # languageAltTitle: 多语言模式下的备用标题
  # 说明: 在语言切换器中显示的语言名称
  # languageAltTitle = "简体中文"

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         主题外观设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # defaultTheme: 默认主题模式
  # 可选值:
  #   - "auto": 自动 (跟随系统主题)
  #   - "light": 浅色模式
  #   - "dark": 深色模式
  defaultTheme = "auto"
  
  # disableThemeToggle: 是否禁用主题切换按钮
  # 说明: true = 隐藏切换按钮, false = 显示切换按钮
  disableThemeToggle = false

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         功能显示开关                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # ShowReadingTime: 是否显示阅读时间
  # 说明: true = 在文章元信息中显示预计阅读时间 (如 "5 min read")
  ShowReadingTime = true
  
  # ShowShareButtons: 是否显示分享按钮
  # 说明: true = 在文章底部显示社交分享按钮
  ShowShareButtons = true
  
  # ShowPostNavLinks: 是否显示上一篇/下一篇导航
  # 说明: true = 在文章底部显示 "上一篇" 和 "下一篇" 链接
  ShowPostNavLinks = true
  
  # ShowBreadCrumbs: 是否显示面包屑导航
  # 说明: true = 在文章标题上方显示导航路径 (如 "首页 > 分类 > 文章")
  ShowBreadCrumbs = true
  
  # ShowCodeCopyButtons: 是否显示代码复制按钮
  # 说明: true = 在代码块右上角显示复制按钮
  ShowCodeCopyButtons = true
  
  # ShowWordCount: 是否显示字数统计
  # 说明: true = 在文章元信息中显示字数
  ShowWordCount = true
  
  # ShowRssButtonInSectionTermList: 是否在列表页显示 RSS 按钮
  # 说明: true = 在分类/标签等列表页显示 RSS 订阅按钮
  ShowRssButtonInSectionTermList = true
  
  # ShowAllPagesInArchive: 是否在归档页显示所有文章
  # 说明: true = 归档页显示所有文章, false = 只显示 posts 目录的文章
  ShowAllPagesInArchive = false
  
  # ShowPageNums: 是否显示页码
  # 说明: true = 在分页导航中显示页码数字
  ShowPageNums = true
  
  # ShowToc: 是否显示目录 (全局设置)
  # 说明: true = 默认显示文章目录, 可在文章中单独覆盖
  ShowToc = true
  
  # UseHugoToc: 是否使用 Hugo 原生目录
  # 说明: true = 使用 Hugo 生成的目录, false = 使用主题自定义目录
  # Hugo 原生目录支持更多功能, 推荐设为 true
  UseHugoToc = true
  
  # ShowFullTextinRSS: 是否在 RSS 中显示完整内容
  # 说明: true = RSS 订阅包含完整文章, false = 只包含摘要
  ShowFullTextinRSS = true

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         功能禁用开关                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # disableSpecial1stPost: 是否禁用首篇文章特殊样式
  # 说明: false = 首页第一篇文章使用大卡片样式, true = 所有文章样式一致
  disableSpecial1stPost = false
  
  # disableScrollToTop: 是否禁用回到顶部按钮
  # 说明: false = 显示右下角的回到顶部按钮
  disableScrollToTop = false
  
  # disableAnchoredHeadings: 是否禁用标题锚点
  # 说明: false = 鼠标悬停在标题上时显示链接图标
  disableAnchoredHeadings = false

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         评论系统设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # comments: 是否启用评论功能
  # 说明: true = 启用评论 (需要配置 layouts/partials/comments.html)
  # 支持的评论系统: Giscus, Utterances, Disqus, Valine 等
  comments = false

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         隐藏元素设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # hidemeta: 是否隐藏文章元信息
  # 说明: true = 隐藏日期、阅读时间、作者等元信息
  hidemeta = false
  
  # hideSummary: 是否隐藏文章摘要
  # 说明: true = 在列表页不显示文章摘要
  hideSummary = false
  
  # hideFooter: 是否隐藏页脚
  # 说明: true = 隐藏页面底部的 footer
  hideFooter = false

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         目录设置                                         │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # showtoc: 是否显示目录 (小写, 用于兼容)
  # 说明: 与 ShowToc 功能相同
  showtoc = true
  
  # tocopen: 目录是否默认展开
  # 说明: true = 页面加载时目录默认展开, false = 默认收起
  tocopen = false

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         内容来源设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # mainSections: 主要内容目录
  # 说明: 首页显示哪些目录下的文章
  # 如果不设置, Hugo 会自动选择文章最多的目录
  mainSections = ["posts"]

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         分享按钮设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # ShareButtons: 自定义分享按钮
  # 说明: 不设置则显示全部可用的分享按钮
  # 可选值: "linkedin", "twitter", "reddit", "facebook", "whatsapp", "telegram"
  # ShareButtons = ["twitter", "linkedin", "reddit", "facebook"]

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         规范链接设置                                     │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # CanonicalLinkText: 规范链接显示文本
  # 说明: 当文章设置了 canonicalURL 时, 显示在链接前的文字
  # CanonicalLinkText = "最初发布于"

  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                         多语言设置                                       │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  # displayFullLangName: 是否显示完整语言名称
  # 说明: true = 语言切换器显示 "简体中文", false = 显示 "zh-cn"
  displayFullLangName = true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                       首页信息模式 (Home-Info Mode)                      ║
  # ║                                                                         ║
  # ║  说明: 首页显示欢迎信息和社交图标                                          ║
  # ║  与 Profile Mode 二选一使用                                              ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.homeInfoParams]
    # Title: 首页标题
    # 说明: 显示在首页中央的大标题
    Title = "👋 Hi there, 欢迎来到 kkkk24 的博客"
    
    # Content: 首页内容
    # 说明: 支持 Markdown 格式
    Content = """
欢迎访问我的个人博客！这里记录技术笔记、学习心得和生活感悟。

- 🔭 正在学习和探索新技术
- 📝 定期分享技术文章
- 💬 欢迎交流讨论
"""

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                       个人资料模式 (Profile Mode)                        ║
  # ║                                                                         ║
  # ║  说明: 首页显示头像、个人介绍和按钮                                        ║
  # ║  与 Home-Info Mode 二选一使用                                            ║
  # ║  取消注释以启用                                                           ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  # [params.profileMode]
  #   # enabled: 是否启用 Profile Mode
  #   # 说明: 必须明确设为 true 才能启用
  #   enabled = false
  #   
  #   # title: 标题 (默认为站点标题)
  #   title = "kkkk24"
  #   
  #   # subtitle: 副标题
  #   subtitle = "技术分享与生活记录"
  #   
  #   # imageUrl: 头像图片 URL
  #   # 说明: 可以是相对路径 (放在 static/) 或外部链接
  #   imageUrl = "/avatar.png"
  #   
  #   # imageWidth: 头像宽度 (像素)
  #   imageWidth = 120
  #   
  #   # imageHeight: 头像高度 (像素)
  #   imageHeight = 120
  #   
  #   # imageTitle: 头像的 alt 文字
  #   imageTitle = "我的头像"
  #   
  #   # buttons: 首页按钮
  #   [[params.profileMode.buttons]]
  #     name = "文章"
  #     url = "/posts/"
  #   [[params.profileMode.buttons]]
  #     name = "标签"
  #     url = "/tags/"
  #   [[params.profileMode.buttons]]
  #     name = "归档"
  #     url = "/archives/"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                            社交图标配置                                   ║
  # ║                                                                         ║
  # ║  可用图标列表: https://github.com/adityatelange/hugo-PaperMod/wiki/Icons ║
  # ║  常用: github, twitter, x, linkedin, facebook, instagram, youtube,      ║
  # ║        telegram, discord, email, rss, mastodon, stackoverflow 等        ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [[params.socialIcons]]
    # name: 图标名称 (必须使用预定义的名称)
    name = "github"
    # url: 链接地址
    url = "https://github.com/kkkk24juastin"
  
  [[params.socialIcons]]
    name = "email"
    url = "mailto:contact@kkkk24juastin.asia"
  
  [[params.socialIcons]]
    name = "rss"
    url = "/index.xml"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                           搜索功能配置                                   ║
  # ║                                                                         ║
  # ║  使用 Fuse.js 实现客户端搜索                                              ║
  # ║  参数说明: https://fusejs.io/api/options.html                           ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.fuseOpts]
    # isCaseSensitive: 是否区分大小写
    # 说明: false = 忽略大小写
    isCaseSensitive = false
    
    # shouldSort: 是否按相关度排序结果
    shouldSort = true
    
    # location: 匹配位置 (0 = 从开头开始)
    location = 0
    
    # distance: 匹配距离阈值
    # 说明: 数值越大, 允许匹配的位置范围越大
    distance = 1000
    
    # threshold: 模糊匹配阈值
    # 说明: 0.0 = 完全匹配, 1.0 = 匹配任何内容
    # 推荐值: 0.4
    threshold = 0.4
    
    # minMatchCharLength: 最小匹配字符数
    # 说明: 输入至少多少字符才开始搜索
    minMatchCharLength = 0
    
    # limit: 最大结果数
    # 说明: 最多显示多少条搜索结果
    limit = 10
    
    # keys: 搜索字段
    # 可选值: "title" (标题), "permalink" (链接), "summary" (摘要), "content" (内容)
    keys = ["title", "permalink", "summary", "content"]

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                           资源文件配置                                   ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.assets]
    # disableHLJS: 是否禁用 Highlight.js
    # 说明: true = 使用 Hugo 内置的 Chroma 语法高亮
    # 设为 true 时需要配置下方的 [markup.highlight]
    # disableHLJS = true
    
    # disableFingerprinting: 是否禁用资源指纹
    # 说明: false = 为 CSS/JS 添加 hash 后缀, 有助于缓存控制
    # 如果遇到资源加载问题, 可以设为 true
    disableFingerprinting = false
    
    # disableScrollBarStyle: 是否禁用自定义滚动条样式
    # disableScrollBarStyle = true
    
    # favicon: 网站图标
    # 说明: 放在 static/ 目录下, 或使用外部 URL
    # 可以使用 https://favicon.io 生成
    # favicon = "/favicon.ico"
    # favicon16x16 = "/favicon-16x16.png"
    # favicon32x32 = "/favicon-32x32.png"
    # apple_touch_icon = "/apple-touch-icon.png"
    # safari_pinned_tab = "/safari-pinned-tab.svg"
    
    # theme_color: 主题颜色
    # 说明: 用于浏览器地址栏颜色 (移动端)
    # theme_color = "#ffffff"
    
    # msapplication_TileColor: Windows 磁贴颜色
    # msapplication_TileColor = "#2d89ef"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                            站点标签配置                                  ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.label]
    # text: 导航栏显示的站点名称
    # 说明: 默认使用站点标题
    text = "kkkk24 的博客"
    
    # icon: 导航栏 Logo 图片
    # 说明: 放在 static/ 目录下
    # icon = "/apple-touch-icon.png"
    
    # iconHeight: Logo 高度 (像素)
    iconHeight = 35

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                           封面图片配置                                   ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.cover]
    # hidden: 是否隐藏封面图
    # 说明: true = 不显示封面图, 但保留在结构化数据中 (SEO)
    hidden = false
    
    # hiddenInList: 是否在列表页隐藏封面
    # 说明: true = 首页/分类页不显示封面, 文章页显示
    hiddenInList = false
    
    # hiddenInSingle: 是否在文章页隐藏封面
    # 说明: true = 文章页不显示封面, 列表页显示
    hiddenInSingle = false
    
    # linkFullImages: 点击封面是否查看大图
    # 说明: true = 点击封面图打开原图
    linkFullImages = true
    
    # responsiveImages: 是否生成响应式图片
    # 说明: true = 自动生成多种尺寸的图片, 优化加载速度
    responsiveImages = true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                           编辑链接配置                                   ║
  # ║                                                                         ║
  # ║  在文章底部显示 "建议修改" 链接, 方便读者提交 PR                            ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  [params.editPost]
    # URL: 编辑链接的基础 URL
    # 说明: 应指向仓库的 content 目录
    # GitHub 格式: https://github.com/用户名/仓库名/edit/分支名/content
    URL = "https://github.com/kkkk24/hugo/edit/main/content"
    
    # Text: 显示的链接文字
    Text = "建议修改"
    
    # appendFilePath: 是否自动追加文件路径
    # 说明: true = 自动追加如 "/posts/my-post.md" 到 URL 后
    appendFilePath = true

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                        网站验证和分析配置                                 ║
  # ║                                                                         ║
  # ║  用于搜索引擎验证站点所有权                                               ║
  # ║  取消注释并填入验证码以启用                                               ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  # [params.analytics.google]
  #   # SiteVerificationTag: Google Search Console 验证码
  #   SiteVerificationTag = "你的Google验证码"
  
  # [params.analytics.bing]
  #   # SiteVerificationTag: Bing Webmaster 验证码
  #   SiteVerificationTag = "你的Bing验证码"
  
  # [params.analytics.yandex]
  #   # SiteVerificationTag: Yandex 验证码
  #   SiteVerificationTag = "你的Yandex验证码"
  
  # [params.analytics.naver]
  #   # SiteVerificationTag: Naver 验证码
  #   SiteVerificationTag = "你的Naver验证码"

  # ╔═════════════════════════════════════════════════════════════════════════╗
  # ║                         Schema.org 结构化数据                            ║
  # ║                                                                         ║
  # ║  用于搜索引擎理解网站内容                                                 ║
  # ╚═════════════════════════════════════════════════════════════════════════╝
  
  # [params.schema]
  #   # publisherType: 发布者类型
  #   # 可选值: "Organization" (组织), "Person" (个人)
  #   publisherType = "Person"
  #   
  #   # sameAs: 相关社交账号链接
  #   # 说明: 用于 https://schema.org/sameAs
  #   sameAs = ["https://github.com/kkkk24"]

# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                        Markdown 渲染配置                                   ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# pygmentsUseClasses: 语法高亮使用 CSS 类
# 说明: true = 使用 CSS 类 (便于主题切换), false = 使用内联样式
pygmentsUseClasses = true

[markup]
  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                        代码高亮配置 (Chroma)                             │
  # │                                                                         │
  # │  完整样式列表: https://xyproto.github.io/splash/docs/                    │
  # │  常用样式: monokai, dracula, github, solarized-dark, vs, xcode          │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  [markup.highlight]
    # anchorLineNos: 是否为行号添加锚点链接
    anchorLineNos = false
    
    # codeFences: 是否启用代码围栏 (```)
    codeFences = true
    
    # guessSyntax: 是否猜测代码语言
    # 说明: true = 没有指定语言时自动猜测
    guessSyntax = true
    
    # hl_Lines: 高亮特定行
    # 示例: "1-3 5" = 高亮第1-3行和第5行
    hl_Lines = ""
    
    # hl_inline: 是否高亮行内代码
    hl_inline = false
    
    # lineAnchors: 行锚点前缀
    lineAnchors = ""
    
    # lineNoStart: 行号起始值
    lineNoStart = 1
    
    # lineNos: 是否显示行号
    lineNos = false
    
    # lineNumbersInTable: 是否使用表格布局行号
    # 说明: true = 行号和代码在不同列, 便于复制代码
    lineNumbersInTable = true
    
    # noClasses: 是否使用内联样式
    # 说明: false = 使用 CSS 类 (推荐), true = 使用内联样式
    noClasses = false
    
    # noHl: 是否禁用高亮
    noHl = false
    
    # style: 代码高亮主题
    # 常用值: monokai, dracula, github, solarized-dark, vs, xcode, nord
    style = "monokai"
    
    # tabWidth: Tab 宽度
    tabWidth = 4
  
  # ┌─────────────────────────────────────────────────────────────────────────┐
  # │                        Goldmark (Markdown 解析器) 配置                   │
  # └─────────────────────────────────────────────────────────────────────────┘
  
  [markup.goldmark]
    [markup.goldmark.renderer]
      # unsafe: 是否允许 HTML
      # 说明: true = 允许在 Markdown 中写 HTML
      unsafe = true

# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                            隐私设置                                        ║
# ║                                                                           ║
# ║  控制第三方服务的隐私行为                                                   ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

[privacy]
  [privacy.vimeo]
    # disabled: 是否禁用 Vimeo 嵌入
    disabled = false
    # simple: 是否使用简单模式 (不加载额外脚本)
    simple = true
  
  [privacy.twitter]
    disabled = false
    # enableDNT: 启用 Do Not Track
    enableDNT = true
    simple = true
  
  [privacy.instagram]
    disabled = false
    simple = true
  
  [privacy.youtube]
    disabled = false
    # privacyEnhanced: 使用隐私增强模式
    # 说明: true = 使用 youtube-nocookie.com
    privacyEnhanced = true

# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                            服务设置                                        ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

[services]
  [services.instagram]
    # disableInlineCSS: 禁用内联 CSS
    disableInlineCSS = true
  
  [services.twitter]
    disableInlineCSS = true
